<configuration>
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} [inst=%X{instanceId} req=%X{requestId} fwd=%X{forwardedFor}] - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/server.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- 每天滚动一次，保留30天 -->
            <fileNamePattern>logs/server.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} [inst=%X{instanceId} req=%X{requestId} fwd=%X{forwardedFor}] - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="BYKC_DEBUG_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/bykc-raw.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/bykc-raw.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>7</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} [inst=%X{instanceId} req=%X{requestId} fwd=%X{forwardedFor}] - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="info">
        <appender-ref ref="STDOUT"/>
        <appender-ref ref="FILE"/>
    </root>
    <logger name="cn.edu.ubaa.bykc.debug" level="INFO" additivity="false">
        <appender-ref ref="BYKC_DEBUG_FILE"/>
    </logger>
    <logger name="io.ktor.server.plugins.cors.routing.CORS" level="WARN"/>
    <logger name="org.eclipse.jetty" level="INFO"/>
    <logger name="io.netty" level="INFO"/>
    <logger name="io.lettuce.core.RedisHandshake" level="WARN"/>
</configuration>
